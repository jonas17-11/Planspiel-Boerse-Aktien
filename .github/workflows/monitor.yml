name: Planspiel-Boerse-Aktien

on:
  workflow_dispatch:      # ➜ manuelles Starten
  push:
    branches:
      - main
  schedule:
    - cron: '0 * * * *'   # ➜ jede Stunde automatisch

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      # 🧩 Repository auschecken
      - name: Checkout repository
        uses: actions/checkout@v3

      # 🐍 Python installieren
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      # 📦 Dependencies installieren
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 📈 monitor.py ausführen (holt aktuelle Kursdaten)
      - name: Run monitor.py
        run: python monitor.py

      # 💬 discord_post.py ausführen (postet Nachricht an Discord)
      - name: Run discord_post.py
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python -c "import sys; \
try: import discord_post; print('✅ discord_post.py executed successfully') \
except Exception as e: print('⚠️ Fehler beim Ausführen von discord_post.py:', e); sys.exit(1)"
